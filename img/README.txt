
File to keep track of empty folder in git.

To delete when project is deployed(Otherwise it appears in the image list).

File to delete in git when project will create img folder on it's own before adding files to it.